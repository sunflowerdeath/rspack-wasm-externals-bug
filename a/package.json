{
  "name": "a",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production rspack --config rspack.config.js && http-server -c-1 ./build",
    "start": "NODE_ENV=production rspack serve --config rspack.config.js",
    "build-webpack": "NODE_ENV=production webpack --config rspack.config.js && http-server -c-1 ./build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "b": "*"
  }
}
